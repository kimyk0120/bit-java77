overflow 
- div에 width값과 height 값을 정해줬을 경우 안에 내용이 overflow 되면 처리하는 css
- overflow: visible;은 기본 default값
- overflow: hidden;은 엘리먼트가 div를 초과하는 부분을 잘라버림
- overflow: scroll; 스크롤 형식으로 바꿔줌 


visibility
- display: none;  영역까지 없어짐
- visibility: hidden; 영역은 남아있는데 안의 엘리먼트가 숨겨짐
 
 
 opacity
 - 이미지의 투명도를 설정한다.
 - 100% = 1.0으로 표시 
 
 position : 속성의 위치 값을 지정해준다.
 - position: absolute;  절대 좌표 지정시 사용
 - top, left, right, bottom으로 위치를 지정  
 - 부모 DIV 태그가 영역을 차지하지 않는다.
 - height 속성의 값을 설정으로 해결
 - position 속성의 값을 relative 로 설정
  - 부모중에 absolute나 relative 유형의 태그가 없다면 body를 기준으로 위치를 찾는다.
  규칙 : 자식의 position이 absolute로 설정하면 부모의 position 속성의 값을 relative로 설정
  - position: fixed;
  /*부모태그 기준이 아니라 body태그 기준으로 위치를 잡는다*/
  
  
z-index: 2;  - 크기가 큰것이 앞쪽에 보여지도록, 순서를 설정한다. 1은 제일밑 2는 그다음..


    float : 
    - 특정 요소를 떠 있게 하는 것, absolute 와 같이 사용 안됨
       떠 있기 위해서 다른 요소를 왼쪽 또는 오른쪽으로 이동시킨다.
       
       
  
  